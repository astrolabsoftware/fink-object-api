name: "e2e: survey=lsst"

on:
  push:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  call-sentinel-template:
    uses: ./.github/workflows/run_test_template.yml
    with:
      survey: lsst
      python-version: "3.11"

