name: "e2e: survey=ztf"

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  call-sentinel-template:
    uses: ./.github/workflows/run_test_template.yml
    with:
      survey: ztf
      python-version: "3.11"

